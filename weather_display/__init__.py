"""
# Description of the weather_display package.

The weather_display package contains all modules and classes related to
the gain, storage and display of weather data from different sources.

# Dependencies and installation

The weather_display package depends on a Python Version >= 3.11 and all
dependencies that are listed in the `requirements.txt`.

All dependencies can be installed with pip using the provided
`requirements.txt` and the command `pip install -r requirements.txt`.

# Usage

The weather_display package can be executed directly with the
command `python -m weather_display` from its parent directory.

# Further remarks

Generating a documentation in HTML format can be achieved by using
`pdoc` and the following commands, issued in the weather_display parent
directory: `pdoc ./weather_display -o docs`.

Author: Jan-Lukas Wolf
"""
